server:
  port: 8081

spring:
  application:
    name: ms-books-catalogue

#  datasource:
#    url: jdbc:h2:mem:bookcatalogue
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
#
#  h2:
#    console:
#      enabled: true
#      path: /h2-console

  datasource:
    url: jdbc:postgresql://srvrelatospapel.postgres.database.azure.com:5432/bookcatalogue
    username: adminrelatos
    password: Relatos2026*
    driver-class-name: org.postgresql.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create-drop  # Crea tablas al arrancar, las borra al cerrar
#    show-sql: true

  jpa:
    hibernate:
      ddl-auto: update  # Crea/actualiza tablas automáticamente
    show-sql: true
    properties:
      hibernate:
        format_sql: true

#logging:
#  level:
#    org.springframework.web: DEBUG

logging:
  level:
    org.hibernate.SQL: DEBUG

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:https://back-end-eureka.railway.internal/eureka}
    register-with-eureka: true
    fetch-registry: true
  instance:
    preferIpAddress: true
    hostname: ms-books-catalogue
